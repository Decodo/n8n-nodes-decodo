export enum TARGET {
  UNIVERSAL = 'universal',
}

export const URL_TARGETS = [TARGET.UNIVERSAL];
export const QUERY_TARGETS = [];
export const HEADLESS_TARGETS = [TARGET.UNIVERSAL];
export const MARKDOWN_TARGETS = [TARGET.UNIVERSAL];
